const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
  theme: {
    extend: {
      fontFamily: {
        recoleta: ['Recoleta M', 'Arial', 'sans-serif'],
      },
    },
  },
};

export default config;
